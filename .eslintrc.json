{
  "extends": [
    "next/core-web-vitals",
    "next/typescript",
    "plugin:prettier/recommended" // (should be last) Including `eslint-config-prettier` here
  ],
  "plugins": ["prettier"],
  "rules": {
    "prettier/prettier": "error" // Make prettier errors show up as eslint errors
  }
}
